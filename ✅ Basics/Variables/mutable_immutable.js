// Mutable vs. Immutable: Mutable means a value can be changed, while immutable means it cannot be changed after creation.

let mutableVar = [1, 2, 3];
mutableVar.push(4); // Mutable, array is modified

const immutableVar = "Hello";
// immutableVar = "World"; // Error, cannot reassign a constant
